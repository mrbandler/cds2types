/**
 * Type tokens.
 *
 * @export
 * @enum {number}
 */
export enum Token {
    export = "export",
    interface = "interface",
    extends = "extends",
    enum = "enum",
    curlyBraceLeft = "{",
    curlyBraceRight = "}",
    colon = ":",
    semiColon = ";",
    equals = "=",
    comma = ",",
    questionMark = "?",
    squareBracketsLeft = "[",
    squareBracketsRight = "]",
}
