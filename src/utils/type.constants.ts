/**
 * Type tokens.
 *
 * @export
 * @enum {number}
 */
export enum TypeToken {
    export = "export",
    interface = "interface",
    extends = "extends",
    enum = "enum",
    curlyBraceLeft = "{",
    curlyBraceRight = "}",
    colon = ":",
    semiColon = ";",
    equals = "=",
    comma = ",",
    questionMark = "?",
    squareBracketsLeft = "[",
    squareBracketsRight = "]",
}
