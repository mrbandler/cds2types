/**
 * Type tokens.
 *
 * @export
 * @enum {number}
 */
export enum TypeToken {
    export = "export",
    interface = "interface",
    enum = "enum",
    curlyBraceLeft = "{",
    curlyBraceRight = "}",
    colon = ":",
    semiColon = ";",
    equals = "=",
    comma = ",",
    questionMark = "?",
    squareBracketsLeft = "[",
    squareBracketsRight = "]",
}
